import "@/styles/userWeb/header.css";
import "@/styles/userWeb/main.css";
import "@/styles/userWeb/join_ac.css";

export default function JoinTermsLayout({
	children,
}: {
	children: React.ReactNode;
}) {
	return <>{children}</>;
}
