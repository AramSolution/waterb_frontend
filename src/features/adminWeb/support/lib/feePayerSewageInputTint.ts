/**
 * 오수량 관련 `<input>` 배경 — RGB(255,224,147)에 **알파 0.1**(약 10% 불투명).
 * 클래스(`bg-gray-100` 등)보다 우선하도록 컴포넌트에서 `style`로 넣는다.
 */
export const FEE_PAYER_SEWAGE_INPUT_BACKGROUND_RGBA =
  "rgba(255, 224, 147, 0.1)" as const;
